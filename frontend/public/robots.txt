# https://siriartsandcrafts.com — set VITE_SITE_URL to your production domain
User-agent: *
Allow: /

# Admin dashboard and API — never index (client-side /admin/* + backend /api/*)
Disallow: /admin
Disallow: /admin/
Disallow: /api
Disallow: /api/

# Dynamic sitemap: generated by the backend API and written to /sitemap.xml on deploy
# (see backend/src/utils/sitemapGenerator.ts). Same-origin URL for production:
Sitemap: https://siriartsandcrafts.com/sitemap.xml
