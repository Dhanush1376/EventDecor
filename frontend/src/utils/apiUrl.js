/**
 * @deprecated Import from `../config/apiConfig` for new code.
 * Re-exports kept for backward compatibility across the codebase.
 */
export { getApiUrl, getApiRootUrl, getApiOrigin, getApiConfig, PRODUCTION_API_ORIGIN } from '../config/apiConfig';
